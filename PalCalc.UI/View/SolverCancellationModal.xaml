<adonisControls:AdonisWindow x:Class="PalCalc.UI.View.SolverCancellationModal"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:itl="clr-namespace:PalCalc.UI.Localization"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             Title="{itl:LocalizedText LC_SOLVER_CANCELLING_TITLE}"
                             ShowInTaskbar="False"
                             ResizeMode="NoResize"
                             WindowStyle="ToolWindow"
                             WindowStartupLocation="CenterOwner"
                             SizeToContent="WidthAndHeight"
                             ShowCloseButton="False"
                             Background="{DynamicResource {x:Static adonisUi:Brushes.Layer3BackgroundBrush}}">
    <StackPanel Orientation="Horizontal" Margin="15">
        <TextBlock Text="{itl:LocalizedText LC_SOLVER_CANCELLING_MESSAGE}"
                   Width="280"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center" />
        <ContentControl ContentTemplate="{DynamicResource {x:Static adonisUi:Templates.LoadingCircle}}"
                        Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                        Width="20"
                        Height="20"
                        Margin="10,0,0,0"
                        Focusable="False" />
    </StackPanel>
</adonisControls:AdonisWindow>
